// 全局配置文件
// 修改这里的IP地址即可切换后端服务
const config = {
  // 后端API基础地址 - 修改这里即可
  baseUrl: 'http://10.33.83.93',
  // 接口前缀
  apiPrefix: '/por-api',
  
  // 超时时间
  timeout: 10000,
  
  // 是否开启调试
  debug: true
}

export default config
